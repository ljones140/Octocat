The upstream remote is usually the repository which you pull from (clone) and then push your changes to.

For example if you are working on a project the upstream remote repo would be on Git Hub. You would clone it locally and work on the code. You would then push you commits to the upstream Git Hub repo. This definition of upstream helps if multi devs are working on same code base as you will know that the central master repo is
